const defaultViews = require('./default-views');
const domain = require('./domain');
const smnExamples = require('./smn-examples');
const testData = require('./test-data');

module.exports = {
    defaultViews,
    domain,
    smnExamples,
    testData
};
